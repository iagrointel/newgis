# Chaves públicas Ed25519 confiáveis para verificar pacote de atualização assinado (item L7-16, ADR 0007
# seção 2). Formato: "<chave_id> <chave_publica_base64> [nota]" — uma linha por chave. A chave PRIVADA
# correspondente NUNCA está neste repositório.
#
# Este arquivo é DADO DE INSTALAÇÃO: quem verifica lê daqui, e quem ASSINA não escreve aqui (endurecimento
# de 06/09/2026, ataque adversarial ao grupo G6 — antes scripts/assinar_pacote.sh acrescentava a própria
# chave nova, o que fazia qualquer pessoa que rodasse a ferramenta virar origem confiável). Acrescentar
# chave é o ato explícito de `bash scripts/confiar_chave_release.sh <id> <publica_b64> --confirmo`, seguido
# de git add + commit + revisão. A única exceção é a ÂNCORA INICIAL: numa instalação cuja lista esteja
# vazia, a primeira chave é registrada com aviso em stderr. Por isso este arquivo NUNCA pode voltar a ficar
# sem chave — tests/unit/test_release_seguranca.py reprova se ficar.
#
# Rotação: a linha da chave nova entra aqui primeiro, numa versão distribuída e instalada com uma
# atualização ASSINADA COM A CHAVE ANTIGA; só depois um pacote assinado com a nova é aceito por
# scripts/verificar_pacote.sh. A mesma lista é a âncora da etiqueta git assinada do release
# (scripts/preparar_release.sh gera var/releases/allowed_signers a partir dela).
kac795e8afb7af013 rRHDs0X8+u/oXMgMnDgQkLrM3KLli3dUIfz5Fa0RtO8= ancora inicial da linha plat, gerada em 2026-09-06T16:01:35Z na maquina de corte de release
