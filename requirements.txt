# Dependências fixadas da aplicação e da suíte (ADR 0001 seção 2.1, alterado em T1).
# Instalar SEMPRE com PYTHONNOUSERSITE=1 (install.sh, Makefile, unidade systemd): a venv tem
# --system-site-packages para reaproveitar os pacotes dpkg, mas nunca depende de ~/.local de ninguém.
# Do sistema, por decisão (pacotes dpkg conferidos pelo install.sh): python3-uvicorn 0.27.1, python3-psycopg2 2.9.9.
# app/rede/isocrona.py (L2-11-c) importa shapely 2.1.2 (concave_hull, GEOS >= 3.11): hoje vem de um pip global
# em /usr/local/lib/.../dist-packages instalado por OUTRO projeto da casa (geopandas o exige), não de dpkg nem
# deste requirements.txt — P5 fica PARCIAL até essa decisão: apt tem candidato python3-shapely 2.0.3 (conferido
# 06/09/2026), mas não foi acrescentado a deploy/pacotes_apt.txt sem o dono decidir (lista fechada, ADR 0007);
# `install.sh` já falha alto se faltar (o self-check final importa app.main, que importa este módulo).
#
# aplicação (app/ importa fastapi, starlette, pydantic, dotenv)
fastapi==0.138.0
starlette==1.3.1
pydantic==2.13.4
pydantic_core==2.46.4
annotated-types==0.8.0
annotated-doc==0.0.5
typing-inspection==0.4.4
typing_extensions==4.15.0
anyio==4.13.0
idna==3.13
python-dotenv==1.2.2
# python-multipart: exigido pelo starlette para `request.form()` com multipart, que é como o cliente Esri
# manda anexo (`addAttachment`/`updateAttachment`/`uploads/upload` do item L2-04-d). Sem ele o starlette
# levanta AssertionError na primeira leitura de formulário. Pacote puro, 30 KB, sem dependência transitiva.
python-multipart==0.0.32
#
# httpx/httpcore: app/rede/osrm.py (L2-11-c) e app/conexao/seguranca.py (L6-02-a) importam direto (não é só
# suíte); httpcore em particular porque app/conexao/seguranca.py monta um httpcore.ConnectionPool próprio com
# network_backend pinado no IP já validado (defesa de SSRF) — depende da API interna estar estável nesta
# versão exata. tests/ também importa os dois (playwright usa httpx por baixo).
httpx==0.28.1
httpcore==1.0.9
h11==0.16.0
certifi==2026.4.22
pytest==9.1.1
pluggy==1.6.0
iniconfig==2.3.0
pytest-base-url==2.1.0
pytest-playwright==0.9.0
playwright==1.59.0
greenlet==3.5.0
pyee==13.0.1
python-slugify==8.0.4
text-unidecode==1.3
ruff==0.16.6
#
# jobs (app/jobs importa croniter)
croniter==6.2.4
python-dateutil==2.9.0.post0
six==1.17.0
#
# identidade (app/auth/totp.py gera o QR do TOTP em SVG; cryptography vem do dpkg python3-cryptography)
qrcode==8.2
#
# LDAP/Active Directory (app/auth/ldap.py; item L0-08-d): puro Python (sem libldap/openldap-dev no sistema);
# pyasn1 já vem do dpkg (python3-pyasn1), a venv só acrescenta o pacote ldap3 em si
ldap3==2.9.1
#
# segurança (scripts/varredura_dependencias.py chama o binário `pip-audit` da venv em subprocesso; item
# L7-03-f, docs/SEGURANCA.md seção 7 justifica a escolha vs `safety`; rich/requests/pygments/markdown-it-py
# que pip-audit também precisa vêm do dpkg — python3-rich, python3-requests, python3-pygments — já instalados
# nesta máquina por outro produto da casa, mesmo raciocínio de uvicorn/psycopg2 do install.sh; se um dia
# faltarem, `pip install -r requirements.txt` os resolve sozinho dentro da venv)
pip-audit==2.10.1
cyclonedx-python-lib==11.12.0
pip-requirements-parser==32.0.1
pip-api==0.0.34
packageurl-python==0.17.6
py-serializable==2.1.0
license-expression==30.4.4
boolean.py==5.0
CacheControl==0.14.4
defusedxml==0.7.1
filelock==3.32.5
msgpack==1.2.2
platformdirs==4.11.7
sortedcontainers==2.4.0
tomli==2.4.1
tomli_w==1.2.0
Markdown==3.10.2

# OpenDSS (item L4-05-a-exportar-opendss): SÓ A SUÍTE importa. A aplicação escreve o `.dss` com a biblioteca
# padrão — o conversor é texto — e nada em `app/` importa opendssdirect. Ele entra aqui porque a cláusula do
# portão do item é "o circuito exportado COMPILA no OpenDSS", e provar isso exige o motor de verdade. Sem ele
# na máquina, `tests/api/test_rede_opendss.py` pula a compilação com a razão escrita, e o resto do teste vale.
# opendssdirect.py 0.9.4 = DSS C-API 0.14.5, a mesma versão do conversor que a casa já rodava.
opendssdirect.py==0.9.4
dss_python==0.15.7
dss_python_backend==0.14.5
#
# leitura de arquivo de feição (GDAL/OGR vetorizado): app/rede_utilidades/bdgd.py e
# app/rede_utilidades/tarefas.py abrem o pacote BDGD da ANEEL (.gdb/.gdb.zip) no job
# `rede.importar_bdgd`. Antes desta linha o pacote vinha do site do usuário (~/.local) e não da venv,
# e `import app.main` quebrava com PYTHONNOUSERSITE=1 (tests/unit/test_dependencias.py). Agora é
# dependência declarada, e o import dela é TARDIO (dentro da função que lê o arquivo) para que só o
# worker do job dependa do GDAL — a API sobe sem ele. numpy/certifi/packaging, que o pyogrio exige,
# vêm de /usr/local/lib/python3.12/dist-packages (mesmo caso do shapely, comentado no topo).
pyogrio==0.12.1

# pandapower (item L4-05-c-pandapower-e-matpower): SÓ A SUÍTE importa, pela mesma razão do
# opendssdirect acima. A aplicação escreve o JSON do pandapower e o `.m` do MATPOWER com a biblioteca
# padrão (os dois são texto) e nada em `app/` importa pandapower. Ele entra aqui porque as cláusulas do
# portão do item são "o arquivo é lido por pp.from_json" e "pp.runpp converge", e provar isso exige o
# motor de verdade.
# ⚠ NÃO instalado na venv compartilhada desta máquina em 08/09/2026: pandapower 3.5.4 exige `pandas~=2.3`
# e a venv está com pandas 3.0.2, então o resolvedor REBAIXARIA pandas e scipy para todo mundo que
# compartilha a venv. A regra do laço manda parar e registrar em vez de instalar. A medida do fluxo foi
# feita num ambiente SEPARADO (fora do repositório) e o teste procura, nesta ordem, o interpretador de
# `PLAT_PYTHON_PANDAPOWER`, o da própria suíte e o do laço; não achando nenhum, PULA com a razão escrita.
# matpowercaseframes é dependência opcional do pandapower para ler `.m` (o `from_mpc` só aceita `.mat`
# sem ela) e serve à conferência cruzada da suíte, nunca ao produto.
pandapower==3.5.4
matpowercaseframes==1.1.1
