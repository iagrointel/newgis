"""Exportação de camada vetorial para outro formato (item L0-04-h-exportar; ADR 0018).

`formatos.py` (o que cada formato é), `motor.py` (consulta segura + ogr2ogr + conversões), `csv_saida.py`
(CSV brasileiro), `erros.py` (erro do banco saneado), `tarefas.py` (job `exportacao.gerar`), `periodicos.py`
(`exportacao.expirar`, validade de 7 dias) e `rotas.py` (a API).
"""
