"""Cena 3D (item L2-09-b-cena-extrusao-slides): o documento de cena é um item do catálogo do tipo
`cena` (mesma tabela, mesmo versionamento e mesmo hash do L0-03/L5-05); aqui ficam só as duas coisas
que o JSON Schema do tipo não expressa — a conferência do documento inteiro (`documento.py`) — e a
conta da posição do Sol para a iluminação (`sol.py`)."""
