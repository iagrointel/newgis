"""Regras de atributo por camada (item L2-10-d-regras-de-atributo): cálculo, restrição, validação e campos
virtuais, avaliados pela linguagem de expressão (app/expressao) no caminho único de escrita (app/edicao) e no job
`camadas.validar`."""
