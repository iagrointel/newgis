"""Versionamento por ramo (item L2-13-a), equivalente ao branch versioning do ArcGIS Enterprise.

`leitura` monta a relação SQL que um ramo (ou um momento histórico do padrão) enxerga; `servico` cria,
reconcilia, resolve, publica e apaga ramo; `edicao` escreve no ramo; `rotas` é a API própria e
`rotas_esri` o VersionManagementServer compatível com o protocolo da Esri."""
